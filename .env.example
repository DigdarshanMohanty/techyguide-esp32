# Compile server URL — used by compileService.js to send Arduino sketches for compilation
# Default: http://localhost:3100/compile (local development)
# Production: set to your hosted compile server URL
COMPILE_SERVER_URL=http://localhost:3100/compile
